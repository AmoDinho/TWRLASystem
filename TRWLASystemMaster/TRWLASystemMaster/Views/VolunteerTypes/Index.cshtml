@model IEnumerable<TRWLASystemMaster.Models.DB.VolunteerType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/VolunteerLoggedInPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VolunteerType_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VolunteerType_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VolunteerTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VolunteerTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VolunteerTypeID })
        </td>
    </tr>
}

</table>
